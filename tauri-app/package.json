{
  "name": "dsh-desktop-aio-tauri",
  "private": true,
  "version": "9.6.3",
  "type": "module",
  "scripts": {
    "build:inject": "node build-inject.mjs",
    "prepare:bundle": "npm run build:inject && npm run sidecar:build && npm --prefix .. run build:native && node ../scripts/sanitize-public-seed.mjs .. && node scripts/stage.ts",
    "tauri": "tauri",
    "dev": "npm run build:inject && npm run sidecar:build && tauri dev",
    "build": "node ../scripts/build-tauri-release.mjs",
    "bundle": "node ../scripts/build-tauri-release.mjs",
    "sidecar:build": "tsc -p ../sidecar/tsconfig.json",
    "sidecar:check": "tsc -p ../sidecar/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.6",
    "@types/node": "^24.13.3",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
