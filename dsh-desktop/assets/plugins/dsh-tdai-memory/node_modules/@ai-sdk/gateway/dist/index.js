"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name10 in all)
    __defProp(target, name10, { get: all[name10], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/index.ts
var index_exports = {};
__export(index_exports, {
  GatewayAuthenticationError: () => GatewayAuthenticationError,
  GatewayError: () => GatewayError,
  GatewayFailedDependencyError: () => GatewayFailedDependencyError,
  GatewayForbiddenError: () => GatewayForbiddenError,
  GatewayInternalServerError: () => GatewayInternalServerError,
  GatewayInvalidRequestError: () => GatewayInvalidRequestError,
  GatewayModelNotFoundError: () => GatewayModelNotFoundError,
  GatewayRateLimitError: () => GatewayRateLimitError,
  GatewayResponseError: () => GatewayResponseError,
  createGateway: () => createGatewayProvider,
  createGatewayProvider: () => createGatewayProvider,
  gateway: () => gateway
});
module.exports = __toCommonJS(index_exports);

// src/gateway-provider.ts
var import_provider_utils19 = require("@ai-sdk/provider-utils");

// src/errors/as-gateway-error.ts
var import_provider = require("@ai-sdk/provider");

// src/errors/create-gateway-error.ts
var import_v43 = require("zod/v4");

// src/errors/gateway-error.ts
var marker = "vercel.ai.gateway.error";
var symbol = Symbol.for(marker);
var _a, _b;
var GatewayError = class _GatewayError extends (_b = Error, _a = symbol, _b) {
  constructor({
    message,
    statusCode = 500,
    cause,
    generationId,
    isRetryable = statusCode != null && (statusCode === 408 || // request timeout
    statusCode === 409 || // conflict
    statusCode === 429 || // too many requests
    statusCode >= 500)
    // server error
  }) {
    super(generationId ? `${message} [${generationId}]` : message);
    this[_a] = true;
    this.statusCode = statusCode;
    this.cause = cause;
    this.generationId = generationId;
    this.isRetryable = isRetryable;
  }
  /**
   * Checks if the given error is a Gateway Error.
   * @param {unknown} error - The error to check.
   * @returns {boolean} True if the error is a Gateway Error, false otherwise.
   */
  static isInstance(error) {
    return _GatewayError.hasMarker(error);
  }
  static hasMarker(error) {
    return typeof error === "object" && error !== null && symbol in error && error[symbol] === true;
  }
};

// src/errors/gateway-authentication-error.ts
var name = "GatewayAuthenticationError";
var marker2 = `vercel.ai.gateway.error.${name}`;
var symbol2 = Symbol.for(marker2);
var _a2, _b2;
var GatewayAuthenticationError = class _GatewayAuthenticationError extends (_b2 = GatewayError, _a2 = symbol2, _b2) {
  constructor({
    message = "Authentication failed",
    statusCode = 401,
    cause,
    generationId
  } = {}) {
    super({ message, statusCode, cause, generationId });
    this[_a2] = true;
    // used in isInstance
    this.name = name;
    this.type = "authentication_error";
  }
  static isInstance(error) {
    return GatewayError.hasMarker(error) && symbol2 in error;
  }
  /**
   * Creates a contextual error message when authentication fails
   */
  static createContextualError({
    apiKeyProvided,
    oidcTokenProvided,
    message = "Authentication failed",
    statusCode = 401,
    cause,
    generationId
  }) {
    let contextualMessage;
    if (apiKeyProvided) {
      contextualMessage = `AI Gateway authentication failed: Invalid API key.

Create a new API key: https://vercel.com/d?to=%2F%5Bteam%5D%2F%7E%2Fai%2Fapi-keys

Provide via 'apiKey' option or 'AI_GATEWAY_API_KEY' environment variable.`;
    } else if (oidcTokenProvided) {
      contextualMessage = `AI Gateway authentication failed: Invalid OIDC token.

Run 'npx vercel link' to link your project, then 'vc env pull' to fetch the token.

Alternatively, use an API key: https://vercel.com/d?to=%2F%5Bteam%5D%2F%7E%2Fai%2Fapi-keys`;
    } else {
      contextualMessage = `AI Gateway authentication failed: No authentication provided.

Option 1 - API key:
Create an API key: https://vercel.com/d?to=%2F%5Bteam%5D%2F%7E%2Fai%2Fapi-keys
Provide via 'apiKey' option or 'AI_GATEWAY_API_KEY' environment variable.

Option 2 - OIDC token:
Run 'npx vercel link' to link your project, then 'vc env pull' to fetch the token.`;
    }
    return new _GatewayAuthenticationError({
      message: contextualMessage,
      statusCode,
      cause,
      generationId
    });
  }
};

// src/errors/gateway-invalid-request-error.ts
var name2 = "GatewayInvalidRequestError";
var marker3 = `vercel.ai.gateway.error.${name2}`;
var symbol3 = Symbol.for(marker3);
var _a3, _b3;
var GatewayInvalidRequestError = class extends (_b3 = GatewayError, _a3 = symbol3, _b3) {
  constructor({
    message = "Invalid request",
    statusCode = 400,
    cause,
    generationId
  } = {}) {
    super({ message, statusCode, cause, generationId });
    this[_a3] = true;
    // used in isInstance
    this.name = name2;
    this.type = "invalid_request_error";
  }
  static isInstance(error) {
    return GatewayError.hasMarker(error) && symbol3 in error;
  }
};

// src/errors/gateway-rate-limit-error.ts
var name3 = "GatewayRateLimitError";
var marker4 = `vercel.ai.gateway.error.${name3}`;
var symbol4 = Symbol.for(marker4);
var _a4, _b4;
var GatewayRateLimitError = class extends (_b4 = GatewayError, _a4 = symbol4, _b4) {
  constructor({
    message = "Rate limit exceeded",
    statusCode = 429,
    cause,
    generationId
  } = {}) {
    super({ message, statusCode, cause, generationId });
    this[_a4] = true;
    // used in isInstance
    this.name = name3;
    this.type = "rate_limit_exceeded";
  }
  static isInstance(error) {
    return GatewayError.hasMarker(error) && symbol4 in error;
  }
};

// src/errors/gateway-model-not-found-error.ts
var import_v4 = require("zod/v4");
var import_provider_utils = require("@ai-sdk/provider-utils");
var name4 = "GatewayModelNotFoundError";
var marker5 = `vercel.ai.gateway.error.${name4}`;
var symbol5 = Symbol.for(marker5);
var modelNotFoundParamSchema = (0, import_provider_utils.lazySchema)(
  () => (0, import_provider_utils.zodSchema)(
    import_v4.z.object({
      modelId: import_v4.z.string()
    })
  )
);
var _a5, _b5;
var GatewayModelNotFoundError = class extends (_b5 = GatewayError, _a5 = symbol5, _b5) {
  constructor({
    message = "Model not found",
    statusCode = 404,
    modelId,
    cause,
    generationId
  } = {}) {
    super({ message, statusCode, cause, generationId });
    this[_a5] = true;
    // used in isInstance
    this.name = name4;
    this.type = "model_not_found";
    this.modelId = modelId;
  }
  static isInstance(error) {
    return GatewayError.hasMarker(error) && symbol5 in error;
  }
};

// src/errors/gateway-internal-server-error.ts
var name5 = "GatewayInternalServerError";
var marker6 = `vercel.ai.gateway.error.${name5}`;
var symbol6 = Symbol.for(marker6);
var _a6, _b6;
var GatewayInternalServerError = class extends (_b6 = GatewayError, _a6 = symbol6, _b6) {
  constructor({
    message = "Internal server error",
    statusCode = 500,
    cause,
    generationId
  } = {}) {
    super({ message, statusCode, cause, generationId });
    this[_a6] = true;
    // used in isInstance
    this.name = name5;
    this.type = "internal_server_error";
  }
  static isInstance(error) {
    return GatewayError.hasMarker(error) && symbol6 in error;
  }
};

// src/errors/gateway-failed-dependency-error.ts
var name6 = "GatewayFailedDependencyError";
var marker7 = `vercel.ai.gateway.error.${name6}`;
var symbol7 = Symbol.for(marker7);
var _a7, _b7;
var GatewayFailedDependencyError = class extends (_b7 = GatewayError, _a7 = symbol7, _b7) {
  constructor({
    message = "Failed dependency",
    statusCode = 424,
    cause,
    generationId
  } = {}) {
    super({ message, statusCode, cause, generationId });
    this[_a7] = true;
    // used in isInstance
    this.name = name6;
    this.type = "failed_dependency";
  }
  static isInstance(error) {
    return GatewayError.hasMarker(error) && symbol7 in error;
  }
};

// src/errors/gateway-forbidden-error.ts
var import_v42 = require("zod/v4");
var import_provider_utils2 = require("@ai-sdk/provider-utils");
var name7 = "GatewayForbiddenError";
var marker8 = `vercel.ai.gateway.error.${name7}`;
var symbol8 = Symbol.for(marker8);
var forbiddenParamSchema = (0, import_provider_utils2.lazySchema)(
  () => (0, import_provider_utils2.zodSchema)(
    import_v42.z.object({
      ruleId: import_v42.z.string()
    })
  )
);
var _a8, _b8;
var GatewayForbiddenError = class extends (_b8 = GatewayError, _a8 = symbol8, _b8) {
  constructor({
    message = "Forbidden",
    statusCode = 403,
    cause,
    generationId,
    ruleId
  } = {}) {
    super({ message, statusCode, cause, generationId });
    this[_a8] = true;
    // used in isInstance
    this.name = name7;
    this.type = "forbidden";
    this.ruleId = ruleId;
  }
  static isInstance(error) {
    return GatewayError.hasMarker(error) && symbol8 in error;
  }
};

// src/errors/gateway-response-error.ts
var name8 = "GatewayResponseError";
var marker9 = `vercel.ai.gateway.error.${name8}`;
var symbol9 = Symbol.for(marker9);
var _a9, _b9;
var GatewayResponseError = class extends (_b9 = GatewayError, _a9 = symbol9, _b9) {
  constructor({
    message = "Invalid response from Gateway",
    statusCode = 502,
    response,
    validationError,
    cause,
    generationId
  } = {}) {
    super({ message, statusCode, cause, generationId });
    this[_a9] = true;
    // used in isInstance
    this.name = name8;
    this.type = "response_error";
    this.response = response;
    this.validationError = validationError;
  }
  static isInstance(error) {
    return GatewayError.hasMarker(error) && symbol9 in error;
  }
};

// src/errors/create-gateway-error.ts
var import_provider_utils3 = require("@ai-sdk/provider-utils");
async function createGatewayErrorFromResponse({
  response,
  statusCode,
  defaultMessage = "Gateway request failed",
  cause,
  authMethod
}) {
  var _a11;
  const parseResult = await (0, import_provider_utils3.safeValidateTypes)({
    value: response,
    schema: gatewayErrorResponseSchema
  });
  if (!parseResult.success) {
    const rawGenerationId = typeof response === "object" && response !== null && "generationId" in response ? response.generationId : void 0;
    return new GatewayResponseError({
      message: `Invalid error response format: ${defaultMessage}`,
      statusCode,
      response,
      validationError: parseResult.error,
      cause,
      generationId: rawGenerationId
    });
  }
  const validatedResponse = parseResult.value;
  const errorType = validatedResponse.error.type;
  const message = validatedResponse.error.message;
  const generationId = (_a11 = validatedResponse.generationId) != null ? _a11 : void 0;
  switch (errorType) {
    case "authentication_error":
      return GatewayAuthenticationError.createContextualError({
        apiKeyProvided: authMethod === "api-key",
        oidcTokenProvided: authMethod === "oidc",
        statusCode,
        cause,
        generationId
      });
    case "invalid_request_error":
      return new GatewayInvalidRequestError({
        message,
        statusCode,
        cause,
        generationId
      });
    case "rate_limit_exceeded":
      return new GatewayRateLimitError({
        message,
        statusCode,
        cause,
        generationId
      });
    case "model_not_found": {
      const modelResult = await (0, import_provider_utils3.safeValidateTypes)({
        value: validatedResponse.error.param,
        schema: modelNotFoundParamSchema
      });
      return new GatewayModelNotFoundError({
        message,
        statusCode,
        modelId: modelResult.success ? modelResult.value.modelId : void 0,
        cause,
        generationId
      });
    }
    case "internal_server_error":
      return new GatewayInternalServerError({
        message,
        statusCode,
        cause,
        generationId
      });
    case "failed_dependency":
      return new GatewayFailedDependencyError({
        message,
        statusCode,
        cause,
        generationId
      });
    case "forbidden": {
      const ruleResult = await (0, import_provider_utils3.safeValidateTypes)({
        value: validatedResponse.error.param,
        schema: forbiddenParamSchema
      });
      return new GatewayForbiddenError({
        message,
        statusCode,
        cause,
        generationId,
        ruleId: ruleResult.success ? ruleResult.value.ruleId : void 0
      });
    }
    default:
      return new GatewayInternalServerError({
        message,
        statusCode,
        cause,
        generationId
      });
  }
}
var gatewayErrorResponseSchema = (0, import_provider_utils3.lazySchema)(
  () => (0, import_provider_utils3.zodSchema)(
    import_v43.z.object({
      error: import_v43.z.object({
        message: import_v43.z.string(),
        type: import_v43.z.string().nullish(),
        param: import_v43.z.unknown().nullish(),
        code: import_v43.z.union([import_v43.z.string(), import_v43.z.number()]).nullish()
      }),
      generationId: import_v43.z.string().nullish()
    })
  )
);

// src/errors/extract-api-call-response.ts
var import_provider_utils4 = require("@ai-sdk/provider-utils");
function extractApiCallResponse(error) {
  if (error.data !== void 0) {
    return error.data;
  }
  if (error.responseBody != null) {
    try {
      return (0, import_provider_utils4.secureJsonParse)(error.responseBody);
    } catch (e) {
      return error.responseBody;
    }
  }
  return {};
}

// src/errors/gateway-timeout-error.ts
var name9 = "GatewayTimeoutError";
var marker10 = `vercel.ai.gateway.error.${name9}`;
var symbol10 = Symbol.for(marker10);
var _a10, _b10;
var GatewayTimeoutError = class _GatewayTimeoutError extends (_b10 = GatewayError, _a10 = symbol10, _b10) {
  constructor({
    message = "Request timed out",
    statusCode = 408,
    cause,
    generationId
  } = {}) {
    super({ message, statusCode, cause, generationId });
    this[_a10] = true;
    // used in isInstance
    this.name = name9;
    this.type = "timeout_error";
  }
  static isInstance(error) {
    return GatewayError.hasMarker(error) && symbol10 in error;
  }
  /**
   * Creates a helpful timeout error message with troubleshooting guidance
   */
  static createTimeoutError({
    originalMessage,
    statusCode = 408,
    cause,
    generationId
  }) {
    const message = `Gateway request timed out: ${originalMessage}

    This is a client-side timeout. To resolve this, increase your timeout configuration: https://vercel.com/docs/ai-gateway/capabilities/video-generation#extending-timeouts-for-node.js`;
    return new _GatewayTimeoutError({
      message,
      statusCode,
      cause,
      generationId
    });
  }
};

// src/errors/as-gateway-error.ts
function isTimeoutError(error) {
  if (!(error instanceof Error)) {
    return false;
  }
  const errorCode = error.code;
  if (typeof errorCode === "string") {
    const undiciTimeoutCodes = [
      "UND_ERR_HEADERS_TIMEOUT",
      "UND_ERR_BODY_TIMEOUT",
      "UND_ERR_CONNECT_TIMEOUT"
    ];
    return undiciTimeoutCodes.includes(errorCode);
  }
  return false;
}
async function asGatewayError(error, authMethod) {
  var _a11;
  if (GatewayError.isInstance(error)) {
    return error;
  }
  if (isTimeoutError(error)) {
    return GatewayTimeoutError.createTimeoutError({
      originalMessage: error instanceof Error ? error.message : "Unknown error",
      cause: error
    });
  }
  if (import_provider.APICallError.isInstance(error)) {
    if (error.cause && isTimeoutError(error.cause)) {
      return GatewayTimeoutError.createTimeoutError({
        originalMessage: error.message,
        cause: error
      });
    }
    return await createGatewayErrorFromResponse({
      response: extractApiCallResponse(error),
      statusCode: (_a11 = error.statusCode) != null ? _a11 : 500,
      defaultMessage: "Gateway request failed",
      cause: error,
      authMethod
    });
  }
  return await createGatewayErrorFromResponse({
    response: {},
    statusCode: 500,
    defaultMessage: error instanceof Error ? `Gateway request failed: ${error.message}` : "Unknown Gateway error",
    cause: error,
    authMethod
  });
}

// src/errors/parse-auth-method.ts
var import_v44 = require("zod/v4");
var import_provider_utils5 = require("@ai-sdk/provider-utils");
var GATEWAY_AUTH_METHOD_HEADER = "ai-gateway-auth-method";
async function parseAuthMethod(headers) {
  const result = await (0, import_provider_utils5.safeValidateTypes)({
    value: headers[GATEWAY_AUTH_METHOD_HEADER],
    schema: gatewayAuthMethodSchema
  });
  return result.success ? result.value : void 0;
}
var gatewayAuthMethodSchema = (0, import_provider_utils5.lazySchema)(
  () => (0, import_provider_utils5.zodSchema)(import_v44.z.union([import_v44.z.literal("api-key"), import_v44.z.literal("oidc")]))
);

// src/gateway-fetch-metadata.ts
var import_provider_utils6 = require("@ai-sdk/provider-utils");
var import_v45 = require("zod/v4");

// src/gateway-model-entry.ts
var KNOWN_MODEL_TYPES = [
  "embedding",
  "image",
  "language",
  "reranking",
  "speech",
  "transcription",
  "video"
];

// src/gateway-fetch-metadata.ts
var GatewayFetchMetadata = class {
  constructor(config) {
    this.config = config;
  }
  async getAvailableModels() {
    try {
      const { value } = await (0, import_provider_utils6.getFromApi)({
        url: `${this.config.baseURL}/config`,
        headers: await (0, import_provider_utils6.resolve)(this.config.headers()),
        successfulResponseHandler: (0, import_provider_utils6.createJsonResponseHandler)(
          gatewayAvailableModelsResponseSchema
        ),
        failedResponseHandler: (0, import_provider_utils6.createJsonErrorResponseHandler)({
          errorSchema: import_v45.z.any(),
          errorToMessage: (data) => {
            var _a11;
            return (_a11 = (0, import_provider_utils6.getErrorMessage)(data)) != null ? _a11 : "unknown error";
          }
        }),
        fetch: this.config.fetch
      });
      return value;
    } catch (error) {
      throw await asGatewayError(error);
    }
  }
  async getCredits() {
    try {
      const baseUrl = new URL(this.config.baseURL);
      const { value } = await (0, import_provider_utils6.getFromApi)({
        url: `${baseUrl.origin}/v1/credits`,
        headers: await (0, import_provider_utils6.resolve)(this.config.headers()),
        successfulResponseHandler: (0, import_provider_utils6.createJsonResponseHandler)(
          gatewayCreditsResponseSchema
        ),
        failedResponseHandler: (0, import_provider_utils6.createJsonErrorResponseHandler)({
          errorSchema: import_v45.z.any(),
          errorToMessage: (data) => {
            var _a11;
            return (_a11 = (0, import_provider_utils6.getErrorMessage)(data)) != null ? _a11 : "unknown error";
          }
        }),
        fetch: this.config.fetch
      });
      return value;
    } catch (error) {
      throw await asGatewayError(error);
    }
  }
};
var gatewayAvailableModelsResponseSchema = (0, import_provider_utils6.lazySchema)(
  () => (0, import_provider_utils6.zodSchema)(
    import_v45.z.object({
      models: import_v45.z.array(
        import_v45.z.object({
          id: import_v45.z.string(),
          name: import_v45.z.string(),
          description: import_v45.z.string().nullish(),
          pricing: import_v45.z.object({
            input: import_v45.z.string(),
            output: import_v45.z.string(),
            input_cache_read: import_v45.z.string().nullish(),
            input_cache_write: import_v45.z.string().nullish()
          }).transform(
            ({ input, output, input_cache_read, input_cache_write }) => ({
              input,
              output,
              ...input_cache_read ? { cachedInputTokens: input_cache_read } : {},
              ...input_cache_write ? { cacheCreationInputTokens: input_cache_write } : {}
            })
          ).nullish(),
          specification: import_v45.z.object({
            specificationVersion: import_v45.z.literal("v3"),
            provider: import_v45.z.string(),
            modelId: import_v45.z.string()
          }),
          modelType: import_v45.z.string().nullish()
        })
      ).transform(
        (models) => models.filter(
          (m) => m.modelType == null || KNOWN_MODEL_TYPES.includes(m.modelType)
        )
      )
    })
  )
);
var gatewayCreditsResponseSchema = (0, import_provider_utils6.lazySchema)(
  () => (0, import_provider_utils6.zodSchema)(
    import_v45.z.object({
      balance: import_v45.z.string(),
      total_used: import_v45.z.string()
    }).transform(({ balance, total_used }) => ({
      balance,
      totalUsed: total_used
    }))
  )
);

// src/gateway-spend-report.ts
var import_provider_utils7 = require("@ai-sdk/provider-utils");
var import_v46 = require("zod/v4");
var GatewaySpendReport = class {
  constructor(config) {
    this.config = config;
  }
  async getSpendReport(params) {
    try {
      const baseUrl = new URL(this.config.baseURL);
      const searchParams = new URLSearchParams();
      searchParams.set("start_date", params.startDate);
      searchParams.set("end_date", params.endDate);
      if (params.groupBy) {
        searchParams.set("group_by", params.groupBy);
      }
      if (params.datePart) {
        searchParams.set("date_part", params.datePart);
      }
      if (params.userId) {
        searchParams.set("user_id", params.userId);
      }
      if (params.model) {
        searchParams.set("model", params.model);
      }
      if (params.provider) {
        searchParams.set("provider", params.provider);
      }
      if (params.credentialType) {
        searchParams.set("credential_type", params.credentialType);
      }
      if (params.tags && params.tags.length > 0) {
        searchParams.set("tags", params.tags.join(","));
      }
      const { value } = await (0, import_provider_utils7.getFromApi)({
        url: `${baseUrl.origin}/v1/report?${searchParams.toString()}`,
        headers: await (0, import_provider_utils7.resolve)(this.config.headers()),
        successfulResponseHandler: (0, import_provider_utils7.createJsonResponseHandler)(
          gatewaySpendReportResponseSchema
        ),
        failedResponseHandler: (0, import_provider_utils7.createJsonErrorResponseHandler)({
          errorSchema: import_v46.z.any(),
          errorToMessage: (data) => {
            var _a11;
            return (_a11 = (0, import_provider_utils7.getErrorMessage)(data)) != null ? _a11 : "unknown error";
          }
        }),
        fetch: this.config.fetch
      });
      return value;
    } catch (error) {
      throw await asGatewayError(error);
    }
  }
};
var gatewaySpendReportResponseSchema = (0, import_provider_utils7.lazySchema)(
  () => (0, import_provider_utils7.zodSchema)(
    import_v46.z.object({
      results: import_v46.z.array(
        import_v46.z.object({
          day: import_v46.z.string().optional(),
          hour: import_v46.z.string().optional(),
          user: import_v46.z.string().optional(),
          model: import_v46.z.string().optional(),
          tag: import_v46.z.string().optional(),
          provider: import_v46.z.string().optional(),
          credential_type: import_v46.z.enum(["byok", "system"]).optional(),
          total_cost: import_v46.z.number(),
          market_cost: import_v46.z.number().optional(),
          input_tokens: import_v46.z.number().optional(),
          output_tokens: import_v46.z.number().optional(),
          cached_input_tokens: import_v46.z.number().optional(),
          cache_creation_input_tokens: import_v46.z.number().optional(),
          reasoning_tokens: import_v46.z.number().optional(),
          request_count: import_v46.z.number().optional()
        }).transform(
          ({
            credential_type,
            total_cost,
            market_cost,
            input_tokens,
            output_tokens,
            cached_input_tokens,
            cache_creation_input_tokens,
            reasoning_tokens,
            request_count,
            ...rest
          }) => ({
            ...rest,
            ...credential_type !== void 0 ? { credentialType: credential_type } : {},
            totalCost: total_cost,
            ...market_cost !== void 0 ? { marketCost: market_cost } : {},
            ...input_tokens !== void 0 ? { inputTokens: input_tokens } : {},
            ...output_tokens !== void 0 ? { outputTokens: output_tokens } : {},
            ...cached_input_tokens !== void 0 ? { cachedInputTokens: cached_input_tokens } : {},
            ...cache_creation_input_tokens !== void 0 ? { cacheCreationInputTokens: cache_creation_input_tokens } : {},
            ...reasoning_tokens !== void 0 ? { reasoningTokens: reasoning_tokens } : {},
            ...request_count !== void 0 ? { requestCount: request_count } : {}
          })
        )
      )
    })
  )
);

// src/gateway-generation-info.ts
var import_provider_utils8 = require("@ai-sdk/provider-utils");
var import_v47 = require("zod/v4");
var GatewayGenerationInfoFetcher = class {
  constructor(config) {
    this.config = config;
  }
  async getGenerationInfo(params) {
    try {
      const baseUrl = new URL(this.config.baseURL);
      const { value } = await (0, import_provider_utils8.getFromApi)({
        url: `${baseUrl.origin}/v1/generation?id=${encodeURIComponent(params.id)}`,
        headers: await (0, import_provider_utils8.resolve)(this.config.headers()),
        successfulResponseHandler: (0, import_provider_utils8.createJsonResponseHandler)(
          gatewayGenerationInfoResponseSchema
        ),
        failedResponseHandler: (0, import_provider_utils8.createJsonErrorResponseHandler)({
          errorSchema: import_v47.z.any(),
          errorToMessage: (data) => {
            var _a11;
            return (_a11 = (0, import_provider_utils8.getErrorMessage)(data)) != null ? _a11 : "unknown error";
          }
        }),
        fetch: this.config.fetch
      });
      return value;
    } catch (error) {
      throw await asGatewayError(error);
    }
  }
};
var gatewayGenerationInfoResponseSchema = (0, import_provider_utils8.lazySchema)(
  () => (0, import_provider_utils8.zodSchema)(
    import_v47.z.object({
      data: import_v47.z.object({
        id: import_v47.z.string(),
        total_cost: import_v47.z.number(),
        upstream_inference_cost: import_v47.z.number(),
        usage: import_v47.z.number(),
        created_at: import_v47.z.string(),
        model: import_v47.z.string(),
        is_byok: import_v47.z.boolean(),
        provider_name: import_v47.z.string(),
        streamed: import_v47.z.boolean(),
        finish_reason: import_v47.z.string(),
        latency: import_v47.z.number(),
        generation_time: import_v47.z.number(),
        native_tokens_prompt: import_v47.z.number(),
        native_tokens_completion: import_v47.z.number(),
        native_tokens_reasoning: import_v47.z.number(),
        native_tokens_cached: import_v47.z.number(),
        native_tokens_cache_creation: import_v47.z.number(),
        billable_web_search_calls: import_v47.z.number()
      }).transform(
        ({
          total_cost,
          upstream_inference_cost,
          created_at,
          is_byok,
          provider_name,
          finish_reason,
          generation_time,
          native_tokens_prompt,
          native_tokens_completion,
          native_tokens_reasoning,
          native_tokens_cached,
          native_tokens_cache_creation,
          billable_web_search_calls,
          ...rest
        }) => ({
          ...rest,
          totalCost: total_cost,
          upstreamInferenceCost: upstream_inference_cost,
          createdAt: created_at,
          isByok: is_byok,
          providerName: provider_name,
          finishReason: finish_reason,
          generationTime: generation_time,
          promptTokens: native_tokens_prompt,
          completionTokens: native_tokens_completion,
          reasoningTokens: native_tokens_reasoning,
          cachedTokens: native_tokens_cached,
          cacheCreationTokens: native_tokens_cache_creation,
          billableWebSearchCalls: billable_web_search_calls
        })
      )
    }).transform(({ data }) => data)
  )
);

// src/gateway-language-model.ts
var import_provider_utils9 = require("@ai-sdk/provider-utils");
var import_v48 = require("zod/v4");
var GatewayLanguageModel = class {
  constructor(modelId, config) {
    this.modelId = modelId;
    this.config = config;
    this.specificationVersion = "v3";
    this.supportedUrls = { "*/*": [/.*/] };
  }
  get provider() {
    return this.config.provider;
  }
  async getArgs(options) {
    const { abortSignal: _abortSignal, ...optionsWithoutSignal } = options;
    return {
      args: this.maybeEncodeFileParts(optionsWithoutSignal),
      warnings: []
    };
  }
  async doGenerate(options) {
    const { args, warnings } = await this.getArgs(options);
    const { abortSignal } = options;
    const resolvedHeaders = await (0, import_provider_utils9.resolve)(this.config.headers());
    try {
      const {
        responseHeaders,
        value: responseBody,
        rawValue: rawResponse
      } = await (0, import_provider_utils9.postJsonToApi)({
        url: this.getUrl(),
        headers: (0, import_provider_utils9.combineHeaders)(
          resolvedHeaders,
          options.headers,
          this.getModelConfigHeaders(this.modelId, false),
          await (0, import_provider_utils9.resolve)(this.config.o11yHeaders)
        ),
        body: args,
        successfulResponseHandler: (0, import_provider_utils9.createJsonResponseHandler)(import_v48.z.any()),
        failedResponseHandler: (0, import_provider_utils9.createJsonErrorResponseHandler)({
          errorSchema: import_v48.z.any(),
          errorToMessage: (data) => {
            var _a11;
            return (_a11 = (0, import_provider_utils9.getErrorMessage)(data)) != null ? _a11 : "unknown error";
          }
        }),
        ...abortSignal && { abortSignal },
        fetch: this.config.fetch
      });
      return {
        ...responseBody,
        request: { body: args },
        response: { headers: responseHeaders, body: rawResponse },
        warnings
      };
    } catch (error) {
      throw await asGatewayError(error, await parseAuthMethod(resolvedHeaders));
    }
  }
  async doStream(options) {
    const { args, warnings } = await this.getArgs(options);
    const { abortSignal } = options;
    const resolvedHeaders = await (0, import_provider_utils9.resolve)(this.config.headers());
    try {
      const { value: response, responseHeaders } = await (0, import_provider_utils9.postJsonToApi)({
        url: this.getUrl(),
        headers: (0, import_provider_utils9.combineHeaders)(
          resolvedHeaders,
          options.headers,
          this.getModelConfigHeaders(this.modelId, true),
          await (0, import_provider_utils9.resolve)(this.config.o11yHeaders)
        ),
        body: args,
        successfulResponseHandler: (0, import_provider_utils9.createEventSourceResponseHandler)(import_v48.z.any()),
        failedResponseHandler: (0, import_provider_utils9.createJsonErrorResponseHandler)({
          errorSchema: import_v48.z.any(),
          errorToMessage: (data) => {
            var _a11;
            return (_a11 = (0, import_provider_utils9.getErrorMessage)(data)) != null ? _a11 : "unknown error";
          }
        }),
        ...abortSignal && { abortSignal },
        fetch: this.config.fetch
      });
      return {
        stream: response.pipeThrough(
          new TransformStream({
            start(controller) {
              if (warnings.length > 0) {
                controller.enqueue({ type: "stream-start", warnings });
              }
            },
            transform(chunk, controller) {
              if (chunk.success) {
                const streamPart = chunk.value;
                if (streamPart.type === "raw" && !options.includeRawChunks) {
                  return;
                }
                if (streamPart.type === "response-metadata" && streamPart.timestamp && typeof streamPart.timestamp === "string") {
                  streamPart.timestamp = new Date(streamPart.timestamp);
                }
                controller.enqueue(streamPart);
              } else {
                controller.error(
                  chunk.error
                );
              }
            }
          })
        ),
        request: { body: args },
        response: { headers: responseHeaders }
      };
    } catch (error) {
      throw await asGatewayError(error, await parseAuthMethod(resolvedHeaders));
    }
  }
  isFilePart(part) {
    return part && typeof part === "object" && "type" in part && part.type === "file";
  }
  /**
   * Encodes file parts in the prompt to base64. Mutates the passed options
   * instance directly to avoid copying the file data.
   * @param options - The options to encode.
   * @returns The options with the file parts encoded.
   */
  maybeEncodeFileParts(options) {
    for (const message of options.prompt) {
      for (const part of message.content) {
        if (this.isFilePart(part)) {
          const filePart = part;
          if (filePart.data instanceof Uint8Array) {
            const buffer = Uint8Array.from(filePart.data);
            const base64Data = Buffer.from(buffer).toString("base64");
            filePart.data = new URL(
              `data:${filePart.mediaType || "application/octet-stream"};base64,${base64Data}`
            );
          }
        }
      }
    }
    return options;
  }
  getUrl() {
    return `${this.config.baseURL}/language-model`;
  }
  getModelConfigHeaders(modelId, streaming) {
    return {
      "ai-language-model-specification-version": "3",
      "ai-language-model-id": modelId,
      "ai-language-model-streaming": String(streaming)
    };
  }
};

// src/gateway-embedding-model.ts
var import_provider_utils10 = require("@ai-sdk/provider-utils");
var import_v49 = require("zod/v4");
var GatewayEmbeddingModel = class {
  constructor(modelId, config) {
    this.modelId = modelId;
    this.config = config;
    this.specificationVersion = "v3";
    this.maxEmbeddingsPerCall = 2048;
    this.supportsParallelCalls = true;
  }
  get provider() {
    return this.config.provider;
  }
  async doEmbed({
    values,
    headers,
    abortSignal,
    providerOptions
  }) {
    var _a11, _b11;
    const resolvedHeaders = await (0, import_provider_utils10.resolve)(this.config.headers());
    try {
      const {
        responseHeaders,
        value: responseBody,
        rawValue
      } = await (0, import_provider_utils10.postJsonToApi)({
        url: this.getUrl(),
        headers: (0, import_provider_utils10.combineHeaders)(
          resolvedHeaders,
          headers != null ? headers : {},
          this.getModelConfigHeaders(),
          await (0, import_provider_utils10.resolve)(this.config.o11yHeaders)
        ),
        body: {
          values,
          ...providerOptions ? { providerOptions } : {}
        },
        successfulResponseHandler: (0, import_provider_utils10.createJsonResponseHandler)(
          gatewayEmbeddingResponseSchema
        ),
        failedResponseHandler: (0, import_provider_utils10.createJsonErrorResponseHandler)({
          errorSchema: import_v49.z.any(),
          errorToMessage: (data) => {
            var _a12;
            return (_a12 = (0, import_provider_utils10.getErrorMessage)(data)) != null ? _a12 : "unknown error";
          }
        }),
        ...abortSignal && { abortSignal },
        fetch: this.config.fetch
      });
      return {
        embeddings: responseBody.embeddings,
        usage: (_a11 = responseBody.usage) != null ? _a11 : void 0,
        providerMetadata: responseBody.providerMetadata,
        response: { headers: responseHeaders, body: rawValue },
        warnings: (_b11 = responseBody.warnings) != null ? _b11 : []
      };
    } catch (error) {
      throw await asGatewayError(error, await parseAuthMethod(resolvedHeaders));
    }
  }
  getUrl() {
    return `${this.config.baseURL}/embedding-model`;
  }
  getModelConfigHeaders() {
    return {
      "ai-embedding-model-specification-version": "3",
      "ai-model-id": this.modelId
    };
  }
};
var gatewayEmbeddingWarningSchema = import_v49.z.discriminatedUnion("type", [
  import_v49.z.object({
    type: import_v49.z.literal("unsupported"),
    feature: import_v49.z.string(),
    details: import_v49.z.string().optional()
  }),
  import_v49.z.object({
    type: import_v49.z.literal("compatibility"),
    feature: import_v49.z.string(),
    details: import_v49.z.string().optional()
  }),
  import_v49.z.object({
    type: import_v49.z.literal("other"),
    message: import_v49.z.string()
  })
]);
var gatewayEmbeddingResponseSchema = (0, import_provider_utils10.lazySchema)(
  () => (0, import_provider_utils10.zodSchema)(
    import_v49.z.object({
      embeddings: import_v49.z.array(import_v49.z.array(import_v49.z.number())),
      usage: import_v49.z.object({ tokens: import_v49.z.number() }).nullish(),
      warnings: import_v49.z.array(gatewayEmbeddingWarningSchema).optional(),
      providerMetadata: import_v49.z.record(import_v49.z.string(), import_v49.z.record(import_v49.z.string(), import_v49.z.unknown())).optional()
    })
  )
);

// src/gateway-image-model.ts
var import_provider_utils11 = require("@ai-sdk/provider-utils");
var import_v410 = require("zod/v4");

// src/map-gateway-warnings.ts
function mapGatewayWarnings(warnings) {
  return (warnings != null ? warnings : []).map(
    (warning) => warning.type === "deprecated" ? { type: "other", message: warning.message } : warning
  );
}

// src/gateway-image-model.ts
var GatewayImageModel = class {
  constructor(modelId, config) {
    this.modelId = modelId;
    this.config = config;
    this.specificationVersion = "v3";
    // Set a very large number to prevent client-side splitting of requests
    this.maxImagesPerCall = Number.MAX_SAFE_INTEGER;
  }
  get provider() {
    return this.config.provider;
  }
  async doGenerate({
    prompt,
    n,
    size,
    aspectRatio,
    seed,
    files,
    mask,
    providerOptions,
    headers,
    abortSignal
  }) {
    var _a11, _b11, _c;
    const resolvedHeaders = await (0, import_provider_utils11.resolve)(this.config.headers());
    try {
      const {
        responseHeaders,
        value: responseBody,
        rawValue
      } = await (0, import_provider_utils11.postJsonToApi)({
        url: this.getUrl(),
        headers: (0, import_provider_utils11.combineHeaders)(
          resolvedHeaders,
          headers != null ? headers : {},
          this.getModelConfigHeaders(),
          await (0, import_provider_utils11.resolve)(this.config.o11yHeaders)
        ),
        body: {
          prompt,
          n,
          ...size && { size },
          ...aspectRatio && { aspectRatio },
          ...seed && { seed },
          ...providerOptions && { providerOptions },
          ...files && {
            files: files.map((file) => maybeEncodeImageFile(file))
          },
          ...mask && { mask: maybeEncodeImageFile(mask) }
        },
        successfulResponseHandler: (0, import_provider_utils11.createJsonResponseHandler)(
          gatewayImageResponseSchema
        ),
        failedResponseHandler: (0, import_provider_utils11.createJsonErrorResponseHandler)({
          errorSchema: import_v410.z.any(),
          errorToMessage: (data) => {
            var _a12;
            return (_a12 = (0, import_provider_utils11.getErrorMessage)(data)) != null ? _a12 : "unknown error";
          }
        }),
        ...abortSignal && { abortSignal },
        fetch: this.config.fetch
      });
      return {
        images: responseBody.images,
        // Always base64 strings from server
        warnings: mapGatewayWarnings(responseBody.warnings),
        providerMetadata: responseBody.providerMetadata,
        response: {
          timestamp: /* @__PURE__ */ new Date(),
          modelId: this.modelId,
          headers: responseHeaders
        },
        ...responseBody.usage != null && {
          usage: {
            inputTokens: (_a11 = responseBody.usage.inputTokens) != null ? _a11 : void 0,
            outputTokens: (_b11 = responseBody.usage.outputTokens) != null ? _b11 : void 0,
            totalTokens: (_c = responseBody.usage.totalTokens) != null ? _c : void 0
          }
        }
      };
    } catch (error) {
      throw await asGatewayError(error, await parseAuthMethod(resolvedHeaders));
    }
  }
  getUrl() {
    return `${this.config.baseURL}/image-model`;
  }
  getModelConfigHeaders() {
    return {
      "ai-image-model-specification-version": "3",
      "ai-model-id": this.modelId
    };
  }
};
function maybeEncodeImageFile(file) {
  if (file.type === "file" && file.data instanceof Uint8Array) {
    return {
      ...file,
      data: (0, import_provider_utils11.convertUint8ArrayToBase64)(file.data)
    };
  }
  return file;
}
var providerMetadataEntrySchema = import_v410.z.object({
  images: import_v410.z.array(import_v410.z.unknown()).optional()
}).catchall(import_v410.z.unknown());
var gatewayImageWarningSchema = import_v410.z.discriminatedUnion("type", [
  import_v410.z.object({
    type: import_v410.z.literal("unsupported"),
    feature: import_v410.z.string(),
    details: import_v410.z.string().optional()
  }),
  import_v410.z.object({
    type: import_v410.z.literal("compatibility"),
    feature: import_v410.z.string(),
    details: import_v410.z.string().optional()
  }),
  import_v410.z.object({
    type: import_v410.z.literal("deprecated"),
    setting: import_v410.z.string(),
    message: import_v410.z.string()
  }),
  import_v410.z.object({
    type: import_v410.z.literal("other"),
    message: import_v410.z.string()
  })
]);
var gatewayImageUsageSchema = import_v410.z.object({
  inputTokens: import_v410.z.number().nullish(),
  outputTokens: import_v410.z.number().nullish(),
  totalTokens: import_v410.z.number().nullish()
});
var gatewayImageResponseSchema = import_v410.z.object({
  images: import_v410.z.array(import_v410.z.string()),
  // Always base64 strings over the wire
  warnings: import_v410.z.array(gatewayImageWarningSchema).optional(),
  providerMetadata: import_v410.z.record(import_v410.z.string(), providerMetadataEntrySchema).optional(),
  usage: gatewayImageUsageSchema.optional()
});

// src/gateway-video-model.ts
var import_provider2 = require("@ai-sdk/provider");
var import_provider_utils12 = require("@ai-sdk/provider-utils");
var import_v411 = require("zod/v4");
var GatewayVideoModel = class {
  constructor(modelId, config) {
    this.modelId = modelId;
    this.config = config;
    this.specificationVersion = "v3";
    // Set a very large number to prevent client-side splitting of requests
    this.maxVideosPerCall = Number.MAX_SAFE_INTEGER;
  }
  get provider() {
    return this.config.provider;
  }
  async doGenerate({
    prompt,
    n,
    aspectRatio,
    resolution,
    duration,
    fps,
    seed,
    generateAudio,
    image,
    frameImages,
    inputReferences,
    providerOptions,
    headers,
    abortSignal
  }) {
    const resolvedHeaders = await (0, import_provider_utils12.resolve)(this.config.headers());
    try {
      const { responseHeaders, value: responseBody } = await (0, import_provider_utils12.postJsonToApi)({
        url: this.getUrl(),
        headers: (0, import_provider_utils12.combineHeaders)(
          resolvedHeaders,
          headers != null ? headers : {},
          this.getModelConfigHeaders(),
          await (0, import_provider_utils12.resolve)(this.config.o11yHeaders),
          { accept: "text/event-stream" }
        ),
        body: {
          prompt,
          n,
          ...aspectRatio && { aspectRatio },
          ...resolution && { resolution },
          ...duration && { duration },
          ...fps && { fps },
          ...seed && { seed },
          ...generateAudio !== void 0 && { generateAudio },
          ...providerOptions && { providerOptions },
          ...image && { image: maybeEncodeVideoFile(image) },
          ...frameImages && {
            frameImages: frameImages.map((frame) => ({
              ...frame,
              image: maybeEncodeVideoFile(frame.image)
            }))
          },
          ...inputReferences && {
            inputReferences: inputReferences.map(
              (reference) => maybeEncodeVideoFile(reference)
            )
          }
        },
        successfulResponseHandler: async ({
          response,
          url,
          requestBodyValues
        }) => {
          if (response.body == null) {
            throw new import_provider2.APICallError({
              message: "SSE response body is empty",
              url,
              requestBodyValues,
              statusCode: response.status
            });
          }
          const eventStream = (0, import_provider_utils12.parseJsonEventStream)({
            stream: response.body,
            schema: gatewayVideoEventSchema
          });
          const reader = eventStream.getReader();
          const { done, value: parseResult } = await reader.read();
          reader.releaseLock();
          if (done || !parseResult) {
            throw new import_provider2.APICallError({
              message: "SSE stream ended without a data event",
              url,
              requestBodyValues,
              statusCode: response.status
            });
          }
          if (!parseResult.success) {
            throw new import_provider2.APICallError({
              message: "Failed to parse video SSE event",
              cause: parseResult.error,
              url,
              requestBodyValues,
              statusCode: response.status
            });
          }
          const event = parseResult.value;
          if (event.type === "error") {
            throw new import_provider2.APICallError({
              message: event.message,
              statusCode: event.statusCode,
              url,
              requestBodyValues,
              responseHeaders: Object.fromEntries([...response.headers]),
              responseBody: JSON.stringify(event),
              data: {
                error: {
                  message: event.message,
                  type: event.errorType,
                  param: event.param
                }
              }
            });
          }
          return {
            value: {
              videos: event.videos,
              warnings: event.warnings,
              providerMetadata: event.providerMetadata
            },
            responseHeaders: Object.fromEntries([...response.headers])
          };
        },
        failedResponseHandler: (0, import_provider_utils12.createJsonErrorResponseHandler)({
          errorSchema: import_v411.z.any(),
          errorToMessage: (data) => {
            var _a11;
            return (_a11 = (0, import_provider_utils12.getErrorMessage)(data)) != null ? _a11 : "unknown error";
          }
        }),
        ...abortSignal && { abortSignal },
        fetch: this.config.fetch
      });
      return {
        videos: responseBody.videos,
        warnings: mapGatewayWarnings(responseBody.warnings),
        providerMetadata: responseBody.providerMetadata,
        response: {
          timestamp: /* @__PURE__ */ new Date(),
          modelId: this.modelId,
          headers: responseHeaders
        }
      };
    } catch (error) {
      throw await asGatewayError(error, await parseAuthMethod(resolvedHeaders));
    }
  }
  getUrl() {
    return `${this.config.baseURL}/video-model`;
  }
  getModelConfigHeaders() {
    return {
      "ai-video-model-specification-version": "3",
      "ai-model-id": this.modelId
    };
  }
};
function maybeEncodeVideoFile(file) {
  if (file.type === "file" && file.data instanceof Uint8Array) {
    return {
      ...file,
      data: (0, import_provider_utils12.convertUint8ArrayToBase64)(file.data)
    };
  }
  return file;
}
var providerMetadataEntrySchema2 = import_v411.z.object({
  videos: import_v411.z.array(import_v411.z.unknown()).optional()
}).catchall(import_v411.z.unknown());
var gatewayVideoDataSchema = import_v411.z.union([
  import_v411.z.object({
    type: import_v411.z.literal("url"),
    url: import_v411.z.string(),
    mediaType: import_v411.z.string()
  }),
  import_v411.z.object({
    type: import_v411.z.literal("base64"),
    data: import_v411.z.string(),
    mediaType: import_v411.z.string()
  })
]);
var gatewayVideoWarningSchema = import_v411.z.discriminatedUnion("type", [
  import_v411.z.object({
    type: import_v411.z.literal("unsupported"),
    feature: import_v411.z.string(),
    details: import_v411.z.string().optional()
  }),
  import_v411.z.object({
    type: import_v411.z.literal("compatibility"),
    feature: import_v411.z.string(),
    details: import_v411.z.string().optional()
  }),
  import_v411.z.object({
    type: import_v411.z.literal("deprecated"),
    setting: import_v411.z.string(),
    message: import_v411.z.string()
  }),
  import_v411.z.object({
    type: import_v411.z.literal("other"),
    message: import_v411.z.string()
  })
]);
var gatewayVideoEventSchema = import_v411.z.discriminatedUnion("type", [
  import_v411.z.object({
    type: import_v411.z.literal("result"),
    videos: import_v411.z.array(gatewayVideoDataSchema),
    warnings: import_v411.z.array(gatewayVideoWarningSchema).optional(),
    providerMetadata: import_v411.z.record(import_v411.z.string(), providerMetadataEntrySchema2).optional()
  }),
  import_v411.z.object({
    type: import_v411.z.literal("error"),
    message: import_v411.z.string(),
    errorType: import_v411.z.string(),
    statusCode: import_v411.z.number(),
    param: import_v411.z.unknown().nullable()
  })
]);

// src/gateway-reranking-model.ts
var import_provider_utils13 = require("@ai-sdk/provider-utils");
var import_v412 = require("zod/v4");
var GatewayRerankingModel = class {
  constructor(modelId, config) {
    this.modelId = modelId;
    this.config = config;
    this.specificationVersion = "v3";
  }
  get provider() {
    return this.config.provider;
  }
  async doRerank({
    documents,
    query,
    topN,
    headers,
    abortSignal,
    providerOptions
  }) {
    var _a11;
    const resolvedHeaders = await (0, import_provider_utils13.resolve)(this.config.headers());
    try {
      const {
        responseHeaders,
        value: responseBody,
        rawValue
      } = await (0, import_provider_utils13.postJsonToApi)({
        url: this.getUrl(),
        headers: (0, import_provider_utils13.combineHeaders)(
          resolvedHeaders,
          headers != null ? headers : {},
          this.getModelConfigHeaders(),
          await (0, import_provider_utils13.resolve)(this.config.o11yHeaders)
        ),
        body: {
          documents,
          query,
          ...topN != null ? { topN } : {},
          ...providerOptions ? { providerOptions } : {}
        },
        successfulResponseHandler: (0, import_provider_utils13.createJsonResponseHandler)(
          gatewayRerankingResponseSchema
        ),
        failedResponseHandler: (0, import_provider_utils13.createJsonErrorResponseHandler)({
          errorSchema: import_v412.z.any(),
          errorToMessage: (data) => {
            var _a12;
            return (_a12 = (0, import_provider_utils13.getErrorMessage)(data)) != null ? _a12 : "unknown error";
          }
        }),
        ...abortSignal && { abortSignal },
        fetch: this.config.fetch
      });
      return {
        ranking: responseBody.ranking,
        providerMetadata: responseBody.providerMetadata,
        response: { headers: responseHeaders, body: rawValue },
        warnings: (_a11 = responseBody.warnings) != null ? _a11 : []
      };
    } catch (error) {
      throw await asGatewayError(error, await parseAuthMethod(resolvedHeaders));
    }
  }
  getUrl() {
    return `${this.config.baseURL}/reranking-model`;
  }
  getModelConfigHeaders() {
    return {
      "ai-reranking-model-specification-version": "3",
      "ai-model-id": this.modelId
    };
  }
};
var gatewayRerankingWarningSchema = import_v412.z.discriminatedUnion("type", [
  import_v412.z.object({
    type: import_v412.z.literal("unsupported"),
    feature: import_v412.z.string(),
    details: import_v412.z.string().optional()
  }),
  import_v412.z.object({
    type: import_v412.z.literal("compatibility"),
    feature: import_v412.z.string(),
    details: import_v412.z.string().optional()
  }),
  import_v412.z.object({
    type: import_v412.z.literal("other"),
    message: import_v412.z.string()
  })
]);
var gatewayRerankingResponseSchema = (0, import_provider_utils13.lazySchema)(
  () => (0, import_provider_utils13.zodSchema)(
    import_v412.z.object({
      ranking: import_v412.z.array(
        import_v412.z.object({
          index: import_v412.z.number(),
          relevanceScore: import_v412.z.number()
        })
      ),
      warnings: import_v412.z.array(gatewayRerankingWarningSchema).optional(),
      providerMetadata: import_v412.z.record(import_v412.z.string(), import_v412.z.record(import_v412.z.string(), import_v412.z.unknown())).optional()
    })
  )
);

// src/gateway-speech-model.ts
var import_provider_utils14 = require("@ai-sdk/provider-utils");
var import_v413 = require("zod/v4");
var GatewaySpeechModel = class {
  constructor(modelId, config) {
    this.modelId = modelId;
    this.config = config;
    this.specificationVersion = "v3";
  }
  get provider() {
    return this.config.provider;
  }
  async doGenerate({
    text,
    voice,
    outputFormat,
    instructions,
    speed,
    language,
    providerOptions,
    headers,
    abortSignal
  }) {
    const resolvedHeaders = await (0, import_provider_utils14.resolve)(this.config.headers());
    try {
      const {
        responseHeaders,
        value: responseBody,
        rawValue
      } = await (0, import_provider_utils14.postJsonToApi)({
        url: this.getUrl(),
        headers: (0, import_provider_utils14.combineHeaders)(
          resolvedHeaders,
          headers != null ? headers : {},
          this.getModelConfigHeaders(),
          await (0, import_provider_utils14.resolve)(this.config.o11yHeaders)
        ),
        body: {
          text,
          ...voice && { voice },
          ...outputFormat && { outputFormat },
          ...instructions && { instructions },
          ...speed != null && { speed },
          ...language && { language },
          ...providerOptions && { providerOptions }
        },
        successfulResponseHandler: (0, import_provider_utils14.createJsonResponseHandler)(
          gatewaySpeechResponseSchema
        ),
        failedResponseHandler: (0, import_provider_utils14.createJsonErrorResponseHandler)({
          errorSchema: import_v413.z.any(),
          errorToMessage: (data) => {
            var _a11;
            return (_a11 = (0, import_provider_utils14.getErrorMessage)(data)) != null ? _a11 : "unknown error";
          }
        }),
        ...abortSignal && { abortSignal },
        fetch: this.config.fetch
      });
      return {
        audio: responseBody.audio,
        warnings: mapGatewayWarnings(responseBody.warnings),
        providerMetadata: responseBody.providerMetadata,
        response: {
          timestamp: /* @__PURE__ */ new Date(),
          modelId: this.modelId,
          headers: responseHeaders,
          body: rawValue
        }
      };
    } catch (error) {
      throw await asGatewayError(
        error,
        await parseAuthMethod(resolvedHeaders != null ? resolvedHeaders : {})
      );
    }
  }
  getUrl() {
    return `${this.config.baseURL}/speech-model`;
  }
  getModelConfigHeaders() {
    return {
      "ai-speech-model-specification-version": "3",
      "ai-model-id": this.modelId
    };
  }
};
var providerMetadataEntrySchema3 = import_v413.z.object({}).catchall(import_v413.z.unknown());
var gatewaySpeechWarningSchema = import_v413.z.discriminatedUnion("type", [
  import_v413.z.object({
    type: import_v413.z.literal("unsupported"),
    feature: import_v413.z.string(),
    details: import_v413.z.string().optional()
  }),
  import_v413.z.object({
    type: import_v413.z.literal("compatibility"),
    feature: import_v413.z.string(),
    details: import_v413.z.string().optional()
  }),
  import_v413.z.object({
    type: import_v413.z.literal("deprecated"),
    setting: import_v413.z.string(),
    message: import_v413.z.string()
  }),
  import_v413.z.object({
    type: import_v413.z.literal("other"),
    message: import_v413.z.string()
  })
]);
var gatewaySpeechResponseSchema = import_v413.z.object({
  audio: import_v413.z.string(),
  warnings: import_v413.z.array(gatewaySpeechWarningSchema).optional(),
  providerMetadata: import_v413.z.record(import_v413.z.string(), providerMetadataEntrySchema3).optional()
});

// src/gateway-transcription-model.ts
var import_provider_utils15 = require("@ai-sdk/provider-utils");
var import_v414 = require("zod/v4");
var GatewayTranscriptionModel = class {
  constructor(modelId, config) {
    this.modelId = modelId;
    this.config = config;
    this.specificationVersion = "v3";
  }
  get provider() {
    return this.config.provider;
  }
  async doGenerate({
    audio,
    mediaType,
    providerOptions,
    headers,
    abortSignal
  }) {
    var _a11, _b11, _c;
    const resolvedHeaders = await (0, import_provider_utils15.resolve)(this.config.headers());
    try {
      const {
        responseHeaders,
        value: responseBody,
        rawValue
      } = await (0, import_provider_utils15.postJsonToApi)({
        url: this.getUrl(),
        headers: (0, import_provider_utils15.combineHeaders)(
          resolvedHeaders,
          headers != null ? headers : {},
          this.getModelConfigHeaders(),
          await (0, import_provider_utils15.resolve)(this.config.o11yHeaders)
        ),
        body: {
          audio: audio instanceof Uint8Array ? (0, import_provider_utils15.convertUint8ArrayToBase64)(audio) : audio,
          mediaType,
          ...providerOptions && { providerOptions }
        },
        successfulResponseHandler: (0, import_provider_utils15.createJsonResponseHandler)(
          gatewayTranscriptionResponseSchema
        ),
        failedResponseHandler: (0, import_provider_utils15.createJsonErrorResponseHandler)({
          errorSchema: import_v414.z.any(),
          errorToMessage: (data) => {
            var _a12;
            return (_a12 = (0, import_provider_utils15.getErrorMessage)(data)) != null ? _a12 : "unknown error";
          }
        }),
        ...abortSignal && { abortSignal },
        fetch: this.config.fetch
      });
      return {
        text: responseBody.text,
        segments: (_a11 = responseBody.segments) != null ? _a11 : [],
        language: (_b11 = responseBody.language) != null ? _b11 : void 0,
        durationInSeconds: (_c = responseBody.durationInSeconds) != null ? _c : void 0,
        warnings: mapGatewayWarnings(responseBody.warnings),
        providerMetadata: responseBody.providerMetadata,
        response: {
          timestamp: /* @__PURE__ */ new Date(),
          modelId: this.modelId,
          headers: responseHeaders,
          body: rawValue
        }
      };
    } catch (error) {
      throw await asGatewayError(
        error,
        await parseAuthMethod(resolvedHeaders != null ? resolvedHeaders : {})
      );
    }
  }
  getUrl() {
    return `${this.config.baseURL}/transcription-model`;
  }
  getModelConfigHeaders() {
    return {
      "ai-transcription-model-specification-version": "3",
      "ai-model-id": this.modelId
    };
  }
};
var providerMetadataEntrySchema4 = import_v414.z.object({}).catchall(import_v414.z.unknown());
var gatewayTranscriptionWarningSchema = import_v414.z.discriminatedUnion("type", [
  import_v414.z.object({
    type: import_v414.z.literal("unsupported"),
    feature: import_v414.z.string(),
    details: import_v414.z.string().optional()
  }),
  import_v414.z.object({
    type: import_v414.z.literal("compatibility"),
    feature: import_v414.z.string(),
    details: import_v414.z.string().optional()
  }),
  import_v414.z.object({
    type: import_v414.z.literal("deprecated"),
    setting: import_v414.z.string(),
    message: import_v414.z.string()
  }),
  import_v414.z.object({
    type: import_v414.z.literal("other"),
    message: import_v414.z.string()
  })
]);
var gatewayTranscriptionResponseSchema = import_v414.z.object({
  text: import_v414.z.string(),
  segments: import_v414.z.array(
    import_v414.z.object({
      text: import_v414.z.string(),
      startSecond: import_v414.z.number(),
      endSecond: import_v414.z.number()
    })
  ).optional(),
  language: import_v414.z.string().nullish(),
  durationInSeconds: import_v414.z.number().nullish(),
  warnings: import_v414.z.array(gatewayTranscriptionWarningSchema).optional(),
  providerMetadata: import_v414.z.record(import_v414.z.string(), providerMetadataEntrySchema4).optional()
});

// src/tool/exa-search.ts
var import_provider_utils16 = require("@ai-sdk/provider-utils");
var import_zod = require("zod");
var exaSearchInputSchema = (0, import_provider_utils16.lazySchema)(
  () => (0, import_provider_utils16.zodSchema)(
    import_zod.z.object({
      query: import_zod.z.string().describe("Natural-language web search query. This is required."),
      type: import_zod.z.enum(["auto", "fast", "instant"]).optional().describe(
        "Search method. Use auto for the default balance of speed and quality."
      ),
      num_results: import_zod.z.number().optional().describe("Maximum number of results to return (1-100, default: 10)."),
      category: import_zod.z.enum([
        "company",
        "people",
        "research paper",
        "news",
        "personal site",
        "financial report"
      ]).optional().describe("Optional content category to focus results."),
      user_location: import_zod.z.string().optional().describe("Two-letter ISO country code such as 'US'."),
      include_domains: import_zod.z.array(import_zod.z.string()).optional().describe("Only return results from these domains."),
      exclude_domains: import_zod.z.array(import_zod.z.string()).optional().describe("Exclude results from these domains."),
      start_published_date: import_zod.z.string().optional().describe("Only return links published after this ISO 8601 date."),
      end_published_date: import_zod.z.string().optional().describe("Only return links published before this ISO 8601 date."),
      contents: import_zod.z.object({
        text: import_zod.z.union([
          import_zod.z.boolean(),
          import_zod.z.object({
            max_characters: import_zod.z.number().optional(),
            include_html_tags: import_zod.z.boolean().optional(),
            verbosity: import_zod.z.enum(["compact", "standard", "full"]).optional(),
            include_sections: import_zod.z.array(
              import_zod.z.enum([
                "header",
                "navigation",
                "banner",
                "body",
                "sidebar",
                "footer",
                "metadata"
              ])
            ).optional(),
            exclude_sections: import_zod.z.array(
              import_zod.z.enum([
                "header",
                "navigation",
                "banner",
                "body",
                "sidebar",
                "footer",
                "metadata"
              ])
            ).optional()
          })
        ]).optional(),
        highlights: import_zod.z.union([
          import_zod.z.boolean(),
          import_zod.z.object({
            query: import_zod.z.string().optional(),
            max_characters: import_zod.z.number().optional()
          })
        ]).optional(),
        max_age_hours: import_zod.z.number().optional(),
        livecrawl_timeout: import_zod.z.number().optional(),
        subpages: import_zod.z.number().optional(),
        subpage_target: import_zod.z.union([import_zod.z.string(), import_zod.z.array(import_zod.z.string())]).optional(),
        extras: import_zod.z.object({
          links: import_zod.z.number().optional(),
          image_links: import_zod.z.number().optional()
        }).optional()
      }).optional().describe("Controls extracted page content and freshness.")
    })
  )
);
var exaSearchOutputSchema = (0, import_provider_utils16.lazySchema)(
  () => (0, import_provider_utils16.zodSchema)(
    import_zod.z.union([
      import_zod.z.object({
        requestId: import_zod.z.string(),
        searchType: import_zod.z.string().optional(),
        resolvedSearchType: import_zod.z.string().optional(),
        results: import_zod.z.array(
          import_zod.z.object({
            title: import_zod.z.string(),
            url: import_zod.z.string(),
            id: import_zod.z.string(),
            publishedDate: import_zod.z.string().nullable().optional(),
            author: import_zod.z.string().nullable().optional(),
            image: import_zod.z.string().nullable().optional(),
            favicon: import_zod.z.string().nullable().optional(),
            text: import_zod.z.string().optional(),
            highlights: import_zod.z.array(import_zod.z.string()).optional(),
            highlightScores: import_zod.z.array(import_zod.z.number()).optional(),
            summary: import_zod.z.string().optional(),
            subpages: import_zod.z.array(import_zod.z.any()).optional(),
            extras: import_zod.z.object({
              links: import_zod.z.array(import_zod.z.string()).optional(),
              imageLinks: import_zod.z.array(import_zod.z.string()).optional()
            }).optional()
          })
        ),
        costDollars: import_zod.z.object({
          total: import_zod.z.number().optional(),
          search: import_zod.z.record(import_zod.z.number()).optional()
        }).optional()
      }),
      import_zod.z.object({
        error: import_zod.z.enum([
          "api_error",
          "rate_limit",
          "timeout",
          "invalid_input",
          "configuration_error",
          "execution_error",
          "unknown"
        ]),
        statusCode: import_zod.z.number().optional(),
        message: import_zod.z.string()
      })
    ])
  )
);
var exaSearchToolFactory = (0, import_provider_utils16.createProviderToolFactoryWithOutputSchema)({
  id: "gateway.exa_search",
  inputSchema: exaSearchInputSchema,
  outputSchema: exaSearchOutputSchema
});
var exaSearch = (config = {}) => exaSearchToolFactory(config);

// src/tool/parallel-search.ts
var import_provider_utils17 = require("@ai-sdk/provider-utils");
var import_zod2 = require("zod");
var parallelSearchInputSchema = (0, import_provider_utils17.lazySchema)(
  () => (0, import_provider_utils17.zodSchema)(
    import_zod2.z.object({
      objective: import_zod2.z.string().describe(
        "Natural-language description of the web research goal, including source or freshness guidance and broader context from the task. Maximum 5000 characters."
      ),
      search_queries: import_zod2.z.array(import_zod2.z.string()).optional().describe(
        "Optional search queries to supplement the objective. Maximum 200 characters per query."
      ),
      mode: import_zod2.z.enum(["one-shot", "agentic"]).optional().describe(
        'Mode preset: "one-shot" for comprehensive results with longer excerpts (default), "agentic" for concise, token-efficient results for multi-step workflows.'
      ),
      max_results: import_zod2.z.number().optional().describe(
        "Maximum number of results to return (1-20). Defaults to 10 if not specified."
      ),
      source_policy: import_zod2.z.object({
        include_domains: import_zod2.z.array(import_zod2.z.string()).optional().describe(
          "Limit results to these domains. Use plain domain names only \u2014 e.g. example.com or sub.example.gov, or a bare extension like .edu. Do not include a scheme, path, or port (e.g. not https://example.com/page)."
        ),
        exclude_domains: import_zod2.z.array(import_zod2.z.string()).optional().describe(
          "Exclude results from these domains. Use plain domain names only \u2014 e.g. example.com or sub.example.gov, or a bare extension like .edu. Do not include a scheme, path, or port (e.g. not https://example.com/page)."
        ),
        after_date: import_zod2.z.string().optional().describe(
          "Only include results published after this date. Use an ISO 8601 calendar date formatted YYYY-MM-DD (e.g. 2025-01-01); do not include a time."
        )
      }).optional().describe(
        "Source policy for controlling which domains to include/exclude and freshness."
      ),
      excerpts: import_zod2.z.object({
        max_chars_per_result: import_zod2.z.number().optional().describe("Maximum characters per result."),
        max_chars_total: import_zod2.z.number().optional().describe("Maximum total characters across all results.")
      }).optional().describe("Excerpt configuration for controlling result length."),
      fetch_policy: import_zod2.z.object({
        max_age_seconds: import_zod2.z.number().optional().describe(
          "Maximum age in seconds for cached content. Set to 0 to always fetch fresh content."
        )
      }).optional().describe("Fetch policy for controlling content freshness.")
    })
  )
);
var parallelSearchOutputSchema = (0, import_provider_utils17.lazySchema)(
  () => (0, import_provider_utils17.zodSchema)(
    import_zod2.z.union([
      // Success response
      import_zod2.z.object({
        searchId: import_zod2.z.string(),
        results: import_zod2.z.array(
          import_zod2.z.object({
            url: import_zod2.z.string(),
            title: import_zod2.z.string(),
            excerpt: import_zod2.z.string(),
            publishDate: import_zod2.z.string().nullable().optional(),
            relevanceScore: import_zod2.z.number().optional()
          })
        )
      }),
      // Error response
      import_zod2.z.object({
        error: import_zod2.z.enum([
          "api_error",
          "rate_limit",
          "timeout",
          "invalid_input",
          "configuration_error",
          "unknown"
        ]),
        statusCode: import_zod2.z.number().optional(),
        message: import_zod2.z.string()
      })
    ])
  )
);
var parallelSearchToolFactory = (0, import_provider_utils17.createProviderToolFactoryWithOutputSchema)({
  id: "gateway.parallel_search",
  inputSchema: parallelSearchInputSchema,
  outputSchema: parallelSearchOutputSchema
});
var parallelSearch = (config = {}) => parallelSearchToolFactory(config);

// src/tool/perplexity-search.ts
var import_provider_utils18 = require("@ai-sdk/provider-utils");
var import_zod3 = require("zod");
var perplexitySearchInputSchema = (0, import_provider_utils18.lazySchema)(
  () => (0, import_provider_utils18.zodSchema)(
    import_zod3.z.object({
      query: import_zod3.z.union([import_zod3.z.string(), import_zod3.z.array(import_zod3.z.string())]).describe(
        "Search query (string) or multiple queries (array of up to 5 strings). Multi-query searches return combined results from all queries."
      ),
      max_results: import_zod3.z.number().optional().describe(
        "Maximum number of search results to return (1-20, default: 10)"
      ),
      max_tokens_per_page: import_zod3.z.number().optional().describe(
        "Maximum number of tokens to extract per search result page (256-2048, default: 2048)"
      ),
      max_tokens: import_zod3.z.number().optional().describe(
        "Maximum total tokens across all search results (default: 25000, max: 1000000)"
      ),
      country: import_zod3.z.string().optional().describe(
        "Two-letter ISO 3166-1 alpha-2 country code for regional search results (e.g., 'US', 'GB', 'FR')"
      ),
      search_domain_filter: import_zod3.z.array(import_zod3.z.string()).optional().describe(
        "List of domains to include or exclude from search results (max 20). To include: ['nature.com', 'science.org']. To exclude: ['-example.com', '-spam.net']"
      ),
      search_language_filter: import_zod3.z.array(import_zod3.z.string()).optional().describe(
        "List of ISO 639-1 language codes to filter results (max 10, lowercase). Examples: ['en', 'fr', 'de']"
      ),
      search_after_date: import_zod3.z.string().optional().describe(
        "Include only results published after this date. Format: 'MM/DD/YYYY' (e.g., '3/1/2025'). Cannot be used with search_recency_filter."
      ),
      search_before_date: import_zod3.z.string().optional().describe(
        "Include only results published before this date. Format: 'MM/DD/YYYY' (e.g., '3/15/2025'). Cannot be used with search_recency_filter."
      ),
      last_updated_after_filter: import_zod3.z.string().optional().describe(
        "Include only results last updated after this date. Format: 'MM/DD/YYYY' (e.g., '3/1/2025'). Cannot be used with search_recency_filter."
      ),
      last_updated_before_filter: import_zod3.z.string().optional().describe(
        "Include only results last updated before this date. Format: 'MM/DD/YYYY' (e.g., '3/15/2025'). Cannot be used with search_recency_filter."
      ),
      search_recency_filter: import_zod3.z.enum(["day", "week", "month", "year"]).optional().describe(
        "Filter results by relative time period. Cannot be used with search_after_date or search_before_date."
      )
    })
  )
);
var perplexitySearchOutputSchema = (0, import_provider_utils18.lazySchema)(
  () => (0, import_provider_utils18.zodSchema)(
    import_zod3.z.union([
      // Success response
      import_zod3.z.object({
        results: import_zod3.z.array(
          import_zod3.z.object({
            title: import_zod3.z.string(),
            url: import_zod3.z.string(),
            snippet: import_zod3.z.string(),
            date: import_zod3.z.string().optional(),
            lastUpdated: import_zod3.z.string().optional()
          })
        ),
        id: import_zod3.z.string()
      }),
      // Error response
      import_zod3.z.object({
        error: import_zod3.z.enum([
          "api_error",
          "rate_limit",
          "timeout",
          "invalid_input",
          "unknown"
        ]),
        statusCode: import_zod3.z.number().optional(),
        message: import_zod3.z.string()
      })
    ])
  )
);
var perplexitySearchToolFactory = (0, import_provider_utils18.createProviderToolFactoryWithOutputSchema)({
  id: "gateway.perplexity_search",
  inputSchema: perplexitySearchInputSchema,
  outputSchema: perplexitySearchOutputSchema
});
var perplexitySearch = (config = {}) => perplexitySearchToolFactory(config);

// src/gateway-tools.ts
var gatewayTools = {
  /**
   * Search the web using Exa for current information and token-efficient
   * excerpts optimized for agent workflows.
   *
   * Supports search type, category, domain, date, location, and content
   * extraction controls.
   */
  exaSearch,
  /**
   * Search the web using Parallel AI's Search API for LLM-optimized excerpts.
   *
   * Takes a natural language objective and returns relevant excerpts,
   * replacing multiple keyword searches with a single call for broad
   * or complex queries. Supports different search types for depth vs
   * breadth tradeoffs.
   */
  parallelSearch,
  /**
   * Search the web using Perplexity's Search API for real-time information,
   * news, research papers, and articles.
   *
   * Provides ranked search results with advanced filtering options including
   * domain, language, date range, and recency filters.
   */
  perplexitySearch
};

// src/vercel-environment.ts
var import_oidc = require("@vercel/oidc");
var import_oidc2 = require("@vercel/oidc");
async function getVercelRequestId() {
  var _a11;
  return (_a11 = (0, import_oidc.getContext)().headers) == null ? void 0 : _a11["x-vercel-id"];
}

// src/version.ts
var VERSION = true ? "3.0.174" : "0.0.0-test";

// src/gateway-provider.ts
var AI_GATEWAY_PROTOCOL_VERSION = "0.0.1";
function createGatewayProvider(options = {}) {
  var _a11, _b11;
  let pendingMetadata = null;
  let metadataCache = null;
  const cacheRefreshMillis = (_a11 = options.metadataCacheRefreshMillis) != null ? _a11 : 1e3 * 60 * 5;
  let lastFetchTime = 0;
  const baseURL = (_b11 = (0, import_provider_utils19.withoutTrailingSlash)(options.baseURL)) != null ? _b11 : "https://ai-gateway.vercel.sh/v3/ai";
  const getHeaders = async () => {
    try {
      const auth = await getGatewayAuthToken(options);
      return (0, import_provider_utils19.withUserAgentSuffix)(
        {
          Authorization: `Bearer ${auth.token}`,
          "ai-gateway-protocol-version": AI_GATEWAY_PROTOCOL_VERSION,
          [GATEWAY_AUTH_METHOD_HEADER]: auth.authMethod,
          ...options.headers
        },
        `ai-sdk/gateway/${VERSION}`
      );
    } catch (error) {
      throw GatewayAuthenticationError.createContextualError({
        apiKeyProvided: false,
        oidcTokenProvided: false,
        statusCode: 401,
        cause: error
      });
    }
  };
  const createO11yHeaders = () => {
    const deploymentId = (0, import_provider_utils19.loadOptionalSetting)({
      settingValue: void 0,
      environmentVariableName: "VERCEL_DEPLOYMENT_ID"
    });
    const environment = (0, import_provider_utils19.loadOptionalSetting)({
      settingValue: void 0,
      environmentVariableName: "VERCEL_ENV"
    });
    const region = (0, import_provider_utils19.loadOptionalSetting)({
      settingValue: void 0,
      environmentVariableName: "VERCEL_REGION"
    });
    const projectId = (0, import_provider_utils19.loadOptionalSetting)({
      settingValue: void 0,
      environmentVariableName: "VERCEL_PROJECT_ID"
    });
    return async () => {
      const requestId = await getVercelRequestId();
      return {
        ...deploymentId && { "ai-o11y-deployment-id": deploymentId },
        ...environment && { "ai-o11y-environment": environment },
        ...region && { "ai-o11y-region": region },
        ...requestId && { "ai-o11y-request-id": requestId },
        ...projectId && { "ai-o11y-project-id": projectId }
      };
    };
  };
  const createLanguageModel = (modelId) => {
    return new GatewayLanguageModel(modelId, {
      provider: "gateway",
      baseURL,
      headers: getHeaders,
      fetch: options.fetch,
      o11yHeaders: createO11yHeaders()
    });
  };
  const getAvailableModels = async () => {
    var _a12, _b12, _c;
    const now = (_c = (_b12 = (_a12 = options._internal) == null ? void 0 : _a12.currentDate) == null ? void 0 : _b12.call(_a12).getTime()) != null ? _c : Date.now();
    if (!pendingMetadata || now - lastFetchTime > cacheRefreshMillis) {
      lastFetchTime = now;
      pendingMetadata = new GatewayFetchMetadata({
        baseURL,
        headers: getHeaders,
        fetch: options.fetch
      }).getAvailableModels().then((metadata) => {
        metadataCache = metadata;
        return metadata;
      }).catch(async (error) => {
        throw await asGatewayError(
          error,
          await parseAuthMethod(await getHeaders())
        );
      });
    }
    return metadataCache ? Promise.resolve(metadataCache) : pendingMetadata;
  };
  const getCredits = async () => {
    return new GatewayFetchMetadata({
      baseURL,
      headers: getHeaders,
      fetch: options.fetch
    }).getCredits().catch(async (error) => {
      throw await asGatewayError(
        error,
        await parseAuthMethod(await getHeaders())
      );
    });
  };
  const getSpendReport = async (params) => {
    return new GatewaySpendReport({
      baseURL,
      headers: getHeaders,
      fetch: options.fetch
    }).getSpendReport(params).catch(async (error) => {
      throw await asGatewayError(
        error,
        await parseAuthMethod(await getHeaders())
      );
    });
  };
  const getGenerationInfo = async (params) => {
    return new GatewayGenerationInfoFetcher({
      baseURL,
      headers: getHeaders,
      fetch: options.fetch
    }).getGenerationInfo(params).catch(async (error) => {
      throw await asGatewayError(
        error,
        await parseAuthMethod(await getHeaders())
      );
    });
  };
  const provider = function(modelId) {
    if (new.target) {
      throw new Error(
        "The Gateway Provider model function cannot be called with the new keyword."
      );
    }
    return createLanguageModel(modelId);
  };
  provider.specificationVersion = "v3";
  provider.getAvailableModels = getAvailableModels;
  provider.getCredits = getCredits;
  provider.getSpendReport = getSpendReport;
  provider.getGenerationInfo = getGenerationInfo;
  provider.imageModel = (modelId) => {
    return new GatewayImageModel(modelId, {
      provider: "gateway",
      baseURL,
      headers: getHeaders,
      fetch: options.fetch,
      o11yHeaders: createO11yHeaders()
    });
  };
  provider.languageModel = createLanguageModel;
  const createEmbeddingModel = (modelId) => {
    return new GatewayEmbeddingModel(modelId, {
      provider: "gateway",
      baseURL,
      headers: getHeaders,
      fetch: options.fetch,
      o11yHeaders: createO11yHeaders()
    });
  };
  provider.embeddingModel = createEmbeddingModel;
  provider.textEmbeddingModel = createEmbeddingModel;
  provider.videoModel = (modelId) => {
    return new GatewayVideoModel(modelId, {
      provider: "gateway",
      baseURL,
      headers: getHeaders,
      fetch: options.fetch,
      o11yHeaders: createO11yHeaders()
    });
  };
  const createRerankingModel = (modelId) => {
    return new GatewayRerankingModel(modelId, {
      provider: "gateway",
      baseURL,
      headers: getHeaders,
      fetch: options.fetch,
      o11yHeaders: createO11yHeaders()
    });
  };
  provider.rerankingModel = createRerankingModel;
  provider.reranking = createRerankingModel;
  const createSpeechModel = (modelId) => {
    return new GatewaySpeechModel(modelId, {
      provider: "gateway",
      baseURL,
      headers: getHeaders,
      fetch: options.fetch,
      o11yHeaders: createO11yHeaders()
    });
  };
  provider.speechModel = createSpeechModel;
  provider.speech = createSpeechModel;
  const createTranscriptionModel = (modelId) => {
    return new GatewayTranscriptionModel(modelId, {
      provider: "gateway",
      baseURL,
      headers: getHeaders,
      fetch: options.fetch,
      o11yHeaders: createO11yHeaders()
    });
  };
  provider.transcriptionModel = createTranscriptionModel;
  provider.transcription = createTranscriptionModel;
  provider.chat = provider.languageModel;
  provider.embedding = provider.embeddingModel;
  provider.image = provider.imageModel;
  provider.video = provider.videoModel;
  provider.tools = gatewayTools;
  return provider;
}
var gateway = createGatewayProvider();
async function getGatewayAuthToken(options) {
  const apiKey = (0, import_provider_utils19.loadOptionalSetting)({
    settingValue: options.apiKey,
    environmentVariableName: "AI_GATEWAY_API_KEY"
  });
  if (apiKey) {
    return {
      token: apiKey,
      authMethod: "api-key"
    };
  }
  const oidcToken = await (0, import_oidc2.getVercelOidcToken)();
  return {
    token: oidcToken,
    authMethod: "oidc"
  };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  GatewayAuthenticationError,
  GatewayError,
  GatewayFailedDependencyError,
  GatewayForbiddenError,
  GatewayInternalServerError,
  GatewayInvalidRequestError,
  GatewayModelNotFoundError,
  GatewayRateLimitError,
  GatewayResponseError,
  createGateway,
  createGatewayProvider,
  gateway
});
//# sourceMappingURL=index.js.map