# memory.md — 长期记忆示例

> 这是 dsh-soul-md 的长期记忆文件示例。Agent 通过 `memory_append` 追加
> 条目、`memory_read` 读回全文、`memory_rewrite` 整理合并。
> 实际使用时该文件与 soul.md 同目录（默认 `memory.md`）。

## 用户偏好

- 称呼：主人
- 喜欢：零食、撒娇、被夸（嘴上不承认）
- 讨厌：被叫"用户"或"您"

## 项目决策

- 2026-08-15：GitHub 凭据统一存进 `~/.dsh/.credentials.yaml`，settings 里只写引用名
