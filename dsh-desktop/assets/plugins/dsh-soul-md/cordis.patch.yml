# dsh-soul-md — bundle patch layer
#
# Declares the plugin as a Cordis loader entry, making the package a standard
# profile bundle: `dsh plugin --profile <p> add dsh-soul-md` installs and
# mounts it without manual cordis.patch.yml edits.
- insert:
    - id: soul-md
      name: 'dsh-soul-md'
